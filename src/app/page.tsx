import { generateMetadata } from '@/lib/seo';

export const metadata = generateMetadata({
  title: 'Luxury Interior Design in Thane & Mumbai',
});

export default function HomePage() {
  return (
    <main>
      <h1>REALSPACE Interiors</h1>
    </main>
  );
}
